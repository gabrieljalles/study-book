//SPREAD
function sum(x,y,z){
    return x,y,z;
}
const numbers = [1,2,3];
sum(...numbers);

// Observe os 3 pontos antes. a função pede para passar 3 numeros exatos
// Caso eu queria enviar um array e os valores do array representando os parametros que função pede.
// Basta que eu use o ...